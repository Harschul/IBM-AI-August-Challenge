# Final PPO checkpoint

The canonical model used by the final demo and benchmark is expected at:

`physical_multisource_stochastic_ppo.zip`

with companion metadata:

`physical_multisource_stochastic_ppo.metadata.json`

This release bundle intentionally does **not** include your locally trained binary
checkpoint. Copying this bundle over the repository will not overwrite it.

Both paths are resolved through `config/final_experiment.json`.
