# Core dependencies for RL environment and training

# Gymnasium for RL environment
gymnasium>=0.29.0

# Stable-Baselines3 for RL algorithms
stable-baselines3>=2.3.0

# SB3-Contrib for MaskablePPO
sb3-contrib>=2.3.0

# Numerical and plotting
numpy>=1.24.0
matplotlib>=3.7.0
pandas>=2.0.0 

# Optional but useful for logging (if you want tensorboard)
tensorboard>=2.13.0

# For graph visualizations (if you use networkx)
networkx>=3.0

# For type checking / dataclasses (already in Python 3.7+)
# but we include it for safety
typing-extensions>=4.5.0

# Test runner. Previously only in the root requirements.txt, which cannot
# install on Python < 3.11 -- so on an older demo machine the test suite
# silently had no way to run.
pytest>=7.0.0
